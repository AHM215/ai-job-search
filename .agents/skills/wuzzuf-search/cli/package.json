{
  "name": "wuzzuf-cli",
  "version": "1.0.0",
  "description": "CLI for searching jobs on Wuzzuf's public browsing pages (Egypt + Saudi Arabia) — no authentication, zero runtime dependencies. Personal use only; respects wuzzuf.net/robots.txt (never uses the disallowed search endpoint, honors Crawl-delay: 10).",
  "type": "module",
  "main": "src/cli.ts",
  "bin": {
    "wuzzuf-search": "src/cli.ts"
  },
  "scripts": {
    "start": "bun run src/cli.ts",
    "test": "bun test --timeout 120000",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {},
  "devDependencies": {
    "typescript": "^5.4.0",
    "@types/bun": "1.3.14"
  }
}
